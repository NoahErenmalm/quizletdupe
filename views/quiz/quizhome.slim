h1 = @quiz["Title"]

h3 Number of questions: #{@quiz["Size"]}

- @questions.each do |question|
    div
        - if question["image"]
            -p question["image"]
            img src="#{question["image"]}" alt="IMG"
        p = question["question"]
        p = question["answer"]
